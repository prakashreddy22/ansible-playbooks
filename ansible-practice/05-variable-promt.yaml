- name: variables
  hosts: web
  become: yes
  # vars:
  #   nginx_port: 80
  # vars_files:
  # #   - vars.yaml
  # vars_prompt:
  #   - name: "nginx_port"
  #     prompt: "Enter the port number for nginx"
  #     private: no
  tasks:
    - name: variable example
      ansible.builtin.debug:
        msg: "nginx will run on port {{ nginx_port }}"
