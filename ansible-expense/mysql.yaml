- name: mysql
  hosts: mysql
  become: yes
  tasks:
    - name: install mysql
      ansible.builtin.yum:
        name: mysql-server
        state: present
    - name: start mysql service
      ansible.builtin.service:
        name: mysqld
        state: started
        enabled: yes
        